[package]
name = "qupath_gpu_tool"
version = "0.1.0"
edition = "2024"

[dependencies]
csv = "1.1"
serde = { version = "1.0", features = ["derive"] }
clap = { version = "4.0", features = ["derive"] }

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }