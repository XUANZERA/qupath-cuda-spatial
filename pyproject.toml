[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "qupath-cuda-spatial"
version = "0.1.0"
description = "GPU accelerated spatial analysis for computational pathology"
requires-python = ">=3.10"